cpu가 있음.
cpu에는 ALU가 있어서 연산을 진행함.

memory 존재함. 4가지 영역(코드, 데이터, 힙, 스택)
보조 memory로 SSD가 있음. 보통 저장소라고 부르는 그거임.

cpu가 메모리에 요청을 하면 보조 SSD가 반응하고 실행됨. 그래서 연산된 결과를 메모리에 저장.
메모리에 저장하니까 자주 계산하는 것은 cpu에 있는게 더 빠르게 계산이 되니까 cpu에 작은
메모리를 저장. 그 친구의 이름이 캐시!

캐시를 지우는 이유는 속도 때문에. 자주 사용하는 것이 아니라면 더미니까.

쿠키는 다른거? ㅇㅇ.. 이제는 권한을 물어봄. 악용하는 사례가 늘어났음. 그래서 정책이 바뀜.
쿠키는 클라이언트에 저장되고 세션은 서버에 저장되어있음. 그래서 로그인하면 일정 시간 및 로그아웃 전까지 로그인이 유지되는 이유

세션이 많아지면 서버에 무리가 감. 그 방안으로 만든 것이 토큰. 메모리를 적게 차지함.
이는 벡엔드에서 다 설정 해야함... 와..

